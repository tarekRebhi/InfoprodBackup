@model MVCWEB.Models.DirectoryViewModel



@*<div class="form-group">
        <div class="col-lg-10">
            @Html.TextBox("test", null, new { @class = "form-control", @id = "typeGenerator" })




        </div>
        <div class="col-md-10">
            @Html.DropDownList("typeGenerator", Model.utilisateurs, htmlAttributes: new { @class = "bootstrap-select", @id = "typeGenerator", @name = "typeGenerator", style = "data-width: 360px;", selected = "selected" })
            @Html.ValidationMessageFor(model => model.utilisateurs, "", new { @class = "text-danger" })
        </div>

    </div>*@
@*<div id="details"></div>*@
<div id="details2"></div>

@*@{
    String nombreDeFichiers = @HttpContext.Current.Session["nombreDeFichiers"].ToString();
    String minutes = @HttpContext.Current.Session["minutes"].ToString();
    String secondes = @HttpContext.Current.Session["secondes"].ToString();

}*@
<div class="content-group" style="margin-left:600px">
    <h5 class="text-semibold no-margin"><i class="icon-calendar5 position-left text-slate"></i> @Model.nbresEnrefgistrements</h5>
    <h5><b>Enregistrements</b></h5>
    <h5 class="text-semibold no-margin"><i class="icon-watch position-left text-slate"></i> @Model.minutes Minutes : @Model.secondes Secondes</h5>
    <h5><b>Temps de recherche</b></h5>
</div>
<div class="content-group">

</div>
<br />
<div class="row">
    @*<span id="notif1" class="badge bg-warning-400">@nombreDeFichiers</span>*@

    @foreach (var item in @Model.files)
    {
        <div class="col-lg-3 col-sm-6">

            <div class="thumbnail" style="height:500px;width:450px;">


                <div class="thumb thumb-rounded">
                    @*<a href="@Url.Action("Details", "Directory", new { enctype = "multipart/form-data" ,Value=item})" @*id="statimg" data-toggle="modal" data-id="@item.Id">*@

                    <a href="@Url.Action("Details", "Directory", new { enctype = "multipart/form-data" ,Value=item})" @*id="statimg" data-toggle="modal" data-id="@item.Id"*@>
                        <img href="#" src="~/Content/smart_folders_icon.png" alt="">
                    </a>
                    @*<div class="caption-overflow">
                            <span>
                                <a href="@item.image" data-popup="lightbox" class="btn border-white text-white btn-flat btn-icon btn-rounded"><i class="icon-plus3"></i></a>
                                <a href="#" class="btn border-white text-white btn-flat btn-icon btn-rounded ml-5"><i class="icon-link2"></i></a>
                            </span>
                        </div>*@

                </div>

                <div class="caption text-center">

                    <h6 class="no-margin-top text-semibold"><a href="#" class="text-default">@Html.DisplayFor(modelItem => item)</a></h6>



                </div>

            </div>

        </div>
    }
    <div class="panel panel-flat">
        <div class="panel-heading">
            <div class="heading-elements">
                @*<ul class="icons-list">
                        <li><a data-action="collapse"></a></li>
                        <li><a data-action="reload"></a></li>
                        <li><a data-action="close"></a></li>
                    </ul>*@
            </div>
        </div>
        <div class="panel-body">
        </div>
        <table class="table datatable-save-state">
            <thead>
                <tr>
                    <th></th>
                    <th>Enregistrement</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>

                @foreach (var item in @Model.filesTestsInfo)
                {
                    <tr>
                        <td><img style="width:20px;height:20px;" src="~/Content/aa.jpg" /></td>
                        <td><a>@item.Name</a></td>
                        <td><button class="btn bg-teal-400" id="test10" data-id="@item.Name" data-FullName="@item.FullName" data-directory="@item.DirectoryName">Ecouter<i class="icon-play"></i></button></td>
                        @*<td>
                            @using (Html.BeginForm("Reab2", "Evaluation", new { enregistrementFullName = item.Name }))
                            {

                                <div class="col-md-2">
                                    <button class="btn bg-blue-300">
                                        <font color="white"></font>Evaluer
                                        <i class="icon-accessibility"></i>
                                    </button>
                                </div>
                            }
                        </td>*@
                        @*<td>

                            <button class="btn bg-blue-300">
                                <font color="white"></font>@Html.ActionLink("Evaluer", "Reab2", "Evaluation", new { enregistrementFullName = item.Name }, new { enctype = "multipart/form-data" })
                                <i class="icon-accessibility"></i>
                            </button>

                        </td>*@
                    </tr>
                                    <tr></tr>
                                    <tr></tr>
                }
            </tbody>
        </table>
    </div>
</div>

    <script type="text/javascript">

        $(document).ready(function () {
            $(document).on("click", "#test10", function () {
                var $buttonClicked = $(this);
                var id = $buttonClicked.attr('data-id');
                var fullname = $buttonClicked.attr('data-FullName');
                var directoryName = $buttonClicked.attr('data-directory');
                $.ajax({
                    url: '/Superviseur/FindCustomEnregistrement',
                    type: 'GET',
                    datatype: 'HTML',
                    data: {
                        'name': id,
                        'fullName': fullname,
                        'DirectoryName': directoryName
                    },
                    async: true,    // execute une fonction asynchrone
                    success: function (data) {
                        //$("#details").innerHTML = "";
                        //$('#detailsFile').removeData();
                        //$('#detailsFile').remove();
                        if ($('#details2').is(':empty')) {
                            //do something
                            $('#details2').html(data);

                           
                        }
                        $('#detailsFile').modal(({
                            backdrop: 'static',
                            keyboard: false,
                        }));
                        //$('#errorFile2').modal('show');

                        //alert("utilisateur"+" "+$("#typeGenerator").val()+" "+""+ $("#datePicker").val())
                    },
                    //complete: function () {
                    //    $('#loadimage').hide();
                    //    $('#details').show();

                    //}
                });
            })
        })
    </script>

