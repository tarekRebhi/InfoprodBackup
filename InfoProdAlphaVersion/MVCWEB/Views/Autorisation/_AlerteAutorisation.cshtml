
<div class="modal fade" id="modifiertest" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true" style="display: none;">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
               <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h5>Autorisation Erreur:</h5>

            </div>

            <div class="modal-body">
                <div class="panel panel-flat">
                    <div class="panel-body">

                        @*<h5>Erreur:</h5>*@
                        Vous ne pouvez pas créer une autorisation qui dépasse les deux heures,ou qui est nulle 
                        assurez vous de respecter ces conditions pour enregistrer une autorisation.
                        @ViewBag.b
                        @ViewBag.C
                         @*@using (Html.BeginForm("DeleteEvent", "Home", new { id = Model.Id }, FormMethod.Get, new { enctype = "multipart/form-data" }))
                            {
                                <input type="submit" value="Delete" class="btn btn-danger" />
                            }*@
                        <!-- /basic radar chart -->
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>